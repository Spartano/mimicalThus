# Migration `20201024162032`

This migration has been generated by The Innovation Factory S.r.l. at 10/24/2020, 6:20:32 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "FakeUsers" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "name" TEXT,
    "email" TEXT,
    "image" TEXT DEFAULT 'http://placeimg.com/640/480/animals',
    "created_at" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
)

PRAGMA foreign_keys=OFF;
CREATE TABLE "new_UserBehavior" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "value" INTEGER NOT NULL,
    "userId" INTEGER NOT NULL,
    "behaviorId" INTEGER NOT NULL,
    "fakeUsersId" INTEGER,

    FOREIGN KEY ("userId") REFERENCES "users"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("behaviorId") REFERENCES "Behavior"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("fakeUsersId") REFERENCES "FakeUsers"("id") ON DELETE SET NULL ON UPDATE CASCADE
);
INSERT INTO "new_UserBehavior" ("id", "value", "userId", "behaviorId") SELECT "id", "value", "userId", "behaviorId" FROM "UserBehavior";
DROP TABLE "UserBehavior";
ALTER TABLE "new_UserBehavior" RENAME TO "UserBehavior";
CREATE TABLE "new_UserPassion" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "value" INTEGER NOT NULL,
    "userId" INTEGER NOT NULL,
    "passionId" INTEGER NOT NULL,
    "fakeUsersId" INTEGER,

    FOREIGN KEY ("userId") REFERENCES "users"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("passionId") REFERENCES "Passion"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("fakeUsersId") REFERENCES "FakeUsers"("id") ON DELETE SET NULL ON UPDATE CASCADE
);
INSERT INTO "new_UserPassion" ("id", "value", "userId", "passionId") SELECT "id", "value", "userId", "passionId" FROM "UserPassion";
DROP TABLE "UserPassion";
ALTER TABLE "new_UserPassion" RENAME TO "UserPassion";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON

CREATE UNIQUE INDEX "FakeUsers.email_unique" ON "FakeUsers"("email")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201024134459..20201024162032
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 model Account {
   id                 Int       @id @default(autoincrement())
@@ -52,8 +52,19 @@
   @@map(name: "users")
 }
+model FakeUsers {
+  id           Int            @id @default(autoincrement())
+  name         String?
+  email        String?        @unique
+  image        String?        @default("http://placeimg.com/640/480/animals")
+  createdAt    DateTime       @default(now()) @map(name: "created_at")
+  updatedAt    DateTime       @default(now()) @map(name: "updated_at")
+  UserPassion  UserPassion[]
+  UserBehavior UserBehavior[]
+}
+
 model VerificationRequest {
   id         Int      @id @default(autoincrement())
   identifier String
   token      String   @unique
@@ -64,23 +75,27 @@
   @@map(name: "verification_requests")
 }
 model UserPassion {
-  id        Int     @id @default(autoincrement())
-  value     Int
-  userId    Int
-  User      User    @relation(fields: [userId], references: [id])
-  passionId Int
-  Passion   Passion @relation(fields: [passionId], references: [id])
+  id          Int        @id @default(autoincrement())
+  value       Int
+  userId      Int
+  User        User       @relation(fields: [userId], references: [id])
+  passionId   Int
+  Passion     Passion    @relation(fields: [passionId], references: [id])
+  FakeUsers   FakeUsers? @relation(fields: [fakeUsersId], references: [id])
+  fakeUsersId Int?
 }
 model UserBehavior {
-  id         Int      @id @default(autoincrement())
-  value      Int
-  User       User     @relation(fields: [userId], references: [id])
-  userId     Int
-  Behavior   Behavior @relation(fields: [behaviorId], references: [id])
-  behaviorId Int
+  id          Int        @id @default(autoincrement())
+  value       Int
+  User        User       @relation(fields: [userId], references: [id])
+  userId      Int
+  Behavior    Behavior   @relation(fields: [behaviorId], references: [id])
+  behaviorId  Int
+  FakeUsers   FakeUsers? @relation(fields: [fakeUsersId], references: [id])
+  fakeUsersId Int?
 }
 model Passion {
   id          Int           @id @default(autoincrement())
```


