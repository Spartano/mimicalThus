# Migration `20201021144100`

This migration has been generated by Ettore bevilacqua at 10/21/2020, 5:41:00 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "User" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "email" TEXT NOT NULL,
    "name" TEXT NOT NULL
)

CREATE TABLE "UserPassion" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "value" INTEGER NOT NULL,
    "userId" INTEGER,
    "passionId" INTEGER,

    FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE,
    FOREIGN KEY ("passionId") REFERENCES "Passion"("id") ON DELETE SET NULL ON UPDATE CASCADE
)

CREATE TABLE "UserBehavior" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "value" INTEGER NOT NULL,
    "userId" INTEGER,
    "behaviorId" INTEGER,

    FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE,
    FOREIGN KEY ("behaviorId") REFERENCES "Behavior"("id") ON DELETE SET NULL ON UPDATE CASCADE
)

CREATE TABLE "Passion" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL
)

CREATE TABLE "Behavior" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL
)

CREATE UNIQUE INDEX "User.email_unique" ON "User"("email")

CREATE UNIQUE INDEX "Passion.name_unique" ON "Passion"("name")

CREATE UNIQUE INDEX "Behavior.name_unique" ON "Behavior"("name")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201021144100
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,48 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "sqlite"
+  url = "***"
+}
+
+model User {
+  id        Int      @id @default(autoincrement())
+  createdAt DateTime @default(now())
+  email     String   @unique
+  name      String
+
+  UserPassion  UserPassion[]
+  UserBehavior UserBehavior[]
+}
+
+model UserPassion {
+  id        Int      @id @default(autoincrement())
+  value     Int
+  userId    Int?
+  User      User?    @relation(fields: [userId], references: [id])
+  passionId Int?
+  Passion   Passion? @relation(fields: [passionId], references: [id])
+}
+
+model UserBehavior {
+  id         Int       @id @default(autoincrement())
+  value      Int
+  User       User?     @relation(fields: [userId], references: [id])
+  userId     Int?
+  Behaviour  Behavior? @relation(fields: [behaviorId], references: [id])
+  behaviorId Int?
+}
+
+model Passion {
+  id          Int           @id @default(autoincrement())
+  name        String        @unique
+  UserPassion UserPassion[]
+}
+
+model Behavior {
+  id           Int            @id @default(autoincrement())
+  name         String         @unique
+  UserBehavior UserBehavior[]
+}
```


